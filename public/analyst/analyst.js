(()=>{'use strict';
const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
let mode='analyst';const history=[];
const json=async(url,options={})=>{const r=await fetch(url,{cache:'no-store',headers:{'content-type':'application/json',...(options.headers||{})},...options});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`HTTP ${r.status}`);return data};
function setStatus(s){const n=$('#analystState');if(!n)return;const model=s?.analyst_model?` · ${s.analyst_model}`:'';n.innerHTML=`<i></i>${s?.configured?'Qwen connecté':'Mode moteur uniquement'}${model}`;n.title=s?.red_team_configured?'Red Team dédié configuré':'Red Team utilisera le moteur ou le modèle analyste configuré';}
function renderWorlds(data){const worlds=data?.worlds||[];$('#worldEntropy').textContent=data?.consensus?.interpretation||'—';$('#worlds').innerHTML=worlds.map((w,i)=>`<article class="pa-world"><header><span>MONDE ${String.fromCharCode(65+i)}</span><b>${Number(w.relative_world_weight_percent||0).toFixed(1)}%</b></header><h3>${esc(w.title)}</h3><div class="prob"><span>Probabilité publique <strong>${esc(w.forecast_probability_percent)}%</strong></span><span>${esc(w.movement_points>=0?`+${w.movement_points}`:w.movement_points)} pts</span></div><div class="bar"><i style="width:${Math.max(2,Math.min(100,Number(w.relative_world_weight_percent||0)))}%"></i></div></article>`).join('')||'<div class="pa-empty">Aucune trajectoire active.</div>';}
function addMessage(role,text,meta=''){const wrap=$('#analystMessages');const article=document.createElement('div');article.className=`pa-message ${role}`;article.innerHTML=`<div class="pa-avatar">${role==='assistant'?'△':'◉'}</div><div><b>${role==='assistant'?'Providence':'Vous'}</b><p>${esc(text)}</p>${meta?`<div class="meta">${esc(meta)}</div>`:''}</div>`;wrap.appendChild(article);wrap.scrollTop=wrap.scrollHeight;}
async function send(message){const clean=String(message||'').trim();if(clean.length<2)return;addMessage('user',clean);history.push({role:'user',content:clean});const btn=$('#analystForm button');btn.disabled=true;document.body.classList.add('pa-busy');try{const data=await json('/api/analyst/chat',{method:'POST',body:JSON.stringify({message:clean,mode,history:history.slice(-8)})});addMessage('assistant',data.text||'Aucune réponse.',`${data.provider||'Providence'}${data.model?` · ${data.model}`:''}${mode==='red_team'?' · RED TEAM':''}`);history.push({role:'assistant',content:data.text||''});if(data.superposition)renderWorlds(data.superposition);}catch(e){addMessage('assistant',`Le moteur conversationnel est momentanément indisponible : ${e.message}`,'dégradé');}finally{btn.disabled=false;document.body.classList.remove('pa-busy');}}
async function boot(){try{const [status,worlds]=await Promise.all([json('/api/analyst/status'),json('/api/superposition')]);setStatus(status);renderWorlds(worlds);}catch(e){$('#analystState').textContent='Providence Analyst indisponible';$('#worlds').innerHTML=`<div class="pa-empty">${esc(e.message)}</div>`;}}
document.querySelectorAll('.pa-mode button').forEach(b=>b.addEventListener('click',()=>{mode=b.dataset.mode||'analyst';document.querySelectorAll('.pa-mode button').forEach(x=>x.classList.toggle('active',x===b));}));document.querySelectorAll('.pa-prompts button').forEach(b=>b.addEventListener('click',()=>{$('#analystInput').value=b.textContent;send(b.textContent);}));$('#analystForm').addEventListener('submit',e=>{e.preventDefault();const input=$('#analystInput');const v=input.value;input.value='';send(v);});$('#analystInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();$('#analystForm').requestSubmit();}});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();