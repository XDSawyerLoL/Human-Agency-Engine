(()=>{'use strict';
if(window.__PROVIDENCE_PREDICTIONS_V1614__)return;window.__PROVIDENCE_PREDICTIONS_V1614__=true;
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[c]));
const H={immediate:'≤ 72 h',near:'Jours à semaines',medium:'Mois',long:'1–3 ans',strategic:'3–5 ans',deep:'5 ans +'};
const D={natural_hazards:'Risques naturels',weather_climate:'Climat & météo',cyber_technology:'Technologie',public_health:'Santé',financial_stress:'Finance',energy:'Énergie',economy_labor:'Économie & emploi',supply_fuel:'Logistique',social_collective_behavior:'Société',geopolitics_security:'Géopolitique',regulation_policy:'Régulation',transport_mobility:'Transport'};
const COLORS=['#ffc85a','#7b7cff','#4f8dff','#46d7ff','#44dda2','#bb7cff','#ff8c72','#8fc8ff'];
const title=f=>String(f?.title||f?.headline||f?.outcome||'Scénario');
const summary=f=>String(f?.summary||f?.public_summary||f?.why_now||'');
const tier=f=>String(f?.horizon_tier||f?.time_window?.tier||'near').toLowerCase();
const prob=f=>Math.max(0,Math.min(100,Math.round(Number(f?.probability?.percent??(Number(f?.probability?.estimate)||0)*100)||0)));
const conf=f=>Math.max(0,Math.min(100,Math.round(Number(f?.consolidation?.score??f?.confidence??0)||0)));
const providers=f=>f?.consolidation?.source_providers||[];
const active=f=>!['resolved','invalidated','expired'].includes(String(f?.status||'').toLowerCase());
const score=f=>prob(f)+conf(f)*.22+Number(f?.commercial_priority||.5)*8+Number(f?.probability_delta_points||0)*1.5;
const clip=(v,n)=>{v=String(v||'').trim();return v.length>n?v.slice(0,n-1)+'…':v};
const looksEnglish=v=>{const s=` ${String(v||'').toLowerCase()} `;return /\b(will|what|when|who|how|world|global|climate change|artificial intelligence|paper published|damage incurred|provide|develop|before|after|between|highest score|public health emergency)\b/.test(s)};
function frRegion(v){let s=String(v||'Monde').trim();const exact={'United States':'États-Unis','United Kingdom':'Royaume-Uni','South Korea':'Corée du Sud','North Korea':'Corée du Nord','European Union':'Union européenne','World':'Monde','Global':'Monde'};if(exact[s])return exact[s];s=s.replace(/^Flood in\s+/i,'Inondations · ').replace(/^Wildfire in\s+/i,'Incendies · ').replace(/^Storm in\s+/i,'Tempête · ').replace(/^Earthquake in\s+/i,'Séisme · ');return s;}
function frTitle(f){const raw=title(f);if(!looksEnglish(raw)&&!/^Découverte scientifique\s*:\s*[A-Z].*\b(of|and|for|with|under)\b/i.test(raw)&&!/^Recherche émergente\s*:\s*[A-Z].*\b(of|and|for|with|under)\b/i.test(raw))return raw;const l=raw.toLowerCase();if(l.includes('greenland'))return 'Les États-Unis obtiendront-ils une souveraineté formelle sur une partie du Groenland en 2026 ?';if(l.includes('climate change')&&l.includes('gdp'))return 'Quel sera le coût économique total du changement climatique au XXIᵉ siècle ?';if(l.includes('nature, science, or cell')||l.includes('formally credited author'))return 'Une IA sera-t-elle créditée comme auteur scientifique dans Nature, Science ou Cell avant 2030 ?';if(l.includes('nuclear fusion')&&l.includes('10%'))return 'Quand la fusion nucléaire fournira-t-elle plus de 10 % de l’énergie primaire mondiale ?';if(l.includes('russia')&&l.includes('ukraine'))return 'Comment évoluera la guerre Russie–Ukraine sur la période suivie ?';if(l.includes('who declare')||l.includes('public health emergency of international concern'))return 'L’OMS déclarera-t-elle une urgence de santé publique de portée internationale ?';if(l.includes('global catastrophe')&&(l.includes('geoengineering')||l.includes('climate')))return 'Si une catastrophe mondiale survient, le climat ou la géo-ingénierie en seront-ils la cause principale ?';if(l.includes('artificial analysis intelligence index')||l.includes('highest score'))return 'Quel sera le meilleur score atteint par l’Artificial Analysis Intelligence Index ?';if(l.includes('first artificial general intelligence')||l.includes('first agi'))return 'Qui développera la première intelligence artificielle générale ?';if(/^découverte scientifique/i.test(raw))return `${D[f.domain]||'Recherche'} : une avancée scientifique récente pourrait ouvrir une nouvelle phase d’application.`;if(/^recherche émergente/i.test(raw))return `${D[f.domain]||'Recherche'} : une nouvelle piste de recherche pourrait franchir une étape d’application.`;return `${D[f.domain]||'Prospective'} · ${frRegion(f?.region||f?.geography||'Monde')} : scénario prospectif à ${H[tier(f)]||'horizon actif'}.`;}
function frSummary(f){const raw=summary(f);if(raw&&!looksEnglish(raw))return raw;return `Providence suit cette trajectoire à partir de signaux convergents et réévalue sa probabilité jusqu’à l’échéance ${H[tier(f)]||'prévue'}.`;}
function diversified(rows,limit=8){const sorted=[...rows].sort((a,b)=>score(b)-score(a)),out=[],seenDomain=new Map(),seenTier=new Map();for(const f of sorted){const d=seenDomain.get(f.domain)||0,h=seenTier.get(tier(f))||0;if(d>=2||h>=3)continue;out.push(f);seenDomain.set(f.domain,d+1);seenTier.set(tier(f),h+1);if(out.length>=limit)break;}for(const f of sorted){if(out.length>=limit)break;if(!out.includes(f))out.push(f);}return out;}
function setHero(f){const delta=Number(f?.signal_convergence?.probability_delta_points??f?.probability_delta_points??0);const hero=$('#futureLead');if(hero)hero.dataset.tone=prob(f)>=70?'high':prob(f)>=45?'mid':'low';const set=(id,v)=>{const n=$(id);if(n)n.textContent=v};set('#futureTitle',frTitle(f));set('#futureProbability',`${prob(f)}%`);set('#futureDelta',delta?`${delta>0?'↑ +':'↓ '}${Math.abs(delta)} pts après convergence`:'Estimation stable');set('#futureRegion',`${frRegion(f?.region||f?.geography||'Monde')} · ${H[tier(f)]||f?.horizon_label||tier(f)}`);set('#futureSummary',frSummary(f));set('#futureConfidence',`${conf(f)||'—'}/100`);set('#futureStrong',String(f?.signal_convergence?.strong_signals?.length||f?.favorable_signals?.length||0));set('#futureSources',String(providers(f).length||'—'));hero?.scrollIntoView({behavior:'smooth',block:'start'});}
function renderTemporal(rows){const host=$('#temporalField'),count=$('#temporalCount');if(!host)return;const selected=diversified(rows,8);if(count)count.textContent=String(rows.length);const W=900,HGT=330,origin={x:74,y:166},xs={immediate:220,near:350,medium:490,long:630,strategic:760,deep:860},ys=[62,98,132,168,204,240,274,302];let svg=`<svg viewBox="0 0 ${W} ${HGT}" role="img" aria-label="Lignes temporelles possibles">`;
 const axes=[['immediate','≤72 h'],['near','Semaines'],['medium','Mois'],['long','1–3 ans'],['strategic','3–5 ans'],['deep','5 ans +']];axes.forEach(([k,label])=>{const x=xs[k];svg+=`<line class="p1614-axis" x1="${x}" y1="28" x2="${x}" y2="314"/><text class="p1614-axis-label" x="${x+5}" y="22">${label}</text>`});svg+=`<circle class="p1614-now-ring" cx="${origin.x}" cy="${origin.y}" r="17"/><circle class="p1614-now-core" cx="${origin.x}" cy="${origin.y}" r="5"/><text class="p1614-now-text" x="38" y="205">MAINTENANT</text>`;
 selected.forEach((f,i)=>{const x=xs[tier(f)]||xs.near,y=ys[i]||160,c=COLORS[i%COLORS.length],w=(2.3+prob(f)/38).toFixed(1),c1=Math.min(x-85,150),c2=Math.max(origin.x+85,x-120),raw=clip(frTitle(f),46),sub=`${prob(f)}% · ${frRegion(f?.region||'Monde')}`;svg+=`<g class="p1614-branch-group" data-branch="${i}" style="--branch:${c};--weight:${w}px"><path class="p1614-branch" d="M ${origin.x} ${origin.y} C ${c1} ${origin.y}, ${c2} ${y}, ${x} ${y}"/><path class="p1614-branch-hit" d="M ${origin.x} ${origin.y} C ${c1} ${origin.y}, ${c2} ${y}, ${x} ${y}"/><circle class="p1614-branch-dot" cx="${x}" cy="${y}" r="7"/><text class="p1614-branch-label" x="${Math.min(x+12,770)}" y="${y-3}">${esc(raw)}</text><text class="p1614-branch-sub" x="${Math.min(x+12,770)}" y="${y+14}">${esc(sub)}</text></g>`});svg+='</svg>';host.innerHTML=svg;$$('.p1614-branch-group',host).forEach((g,i)=>{const activate=()=>{$$('.p1614-branch-group',host).forEach(x=>x.classList.toggle('is-muted',x!==g));g.classList.remove('is-muted');g.classList.add('is-active');setHero(selected[i]);};g.addEventListener('click',activate);g.addEventListener('mouseenter',()=>g.classList.add('is-active'));g.addEventListener('mouseleave',()=>g.classList.remove('is-active'));});}
function localizeRendered(rows){const titleMap=new Map(rows.map(f=>[title(f).trim(),frTitle(f)])),summaryMap=new Map(rows.map(f=>[summary(f).trim(),frSummary(f)]));const apply=()=>{$$('.v4-card h3,.p15-forecast-card h3').forEach(n=>{const k=n.textContent.trim();if(titleMap.has(k))n.textContent=titleMap.get(k)});$$('.v4-summary').forEach(n=>{const k=n.textContent.trim();if(summaryMap.has(k))n.textContent=summaryMap.get(k)});const regionNode=$('#futureRegion');if(regionNode){regionNode.textContent=regionNode.textContent.replace('United States','États-Unis').replace('United Kingdom','Royaume-Uni').replace(/Flood in\s+/i,'Inondations · ')}};apply();const mo=new MutationObserver(apply);mo.observe(document.body,{childList:true,subtree:true});setTimeout(()=>mo.disconnect(),10000);}
async function boot(){try{const r=await fetch('/api/snapshot',{cache:'no-store'});if(!r.ok)return;const data=await r.json(),rows=(data.forecasts||[]).filter(active).sort((a,b)=>score(b)-score(a));renderTemporal(rows);localizeRendered(rows);const note=$('.p16-secondary-heading h2');if(note&&!note.parentElement.querySelector('.p1614-french-note'))note.insertAdjacentHTML('afterend','<span class="p1614-french-note">affichage public normalisé</span>');}catch{}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();