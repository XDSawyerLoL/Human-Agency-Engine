(()=>{'use strict';
if(window.__PROVIDENCE_PREDICTIONS_V1615__)return;window.__PROVIDENCE_PREDICTIONS_V1615__=true;
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[c]));
const H={immediate:'≤ 72 h',near:'Jours à semaines',medium:'Mois',long:'1–3 ans',strategic:'3–5 ans',deep:'5 ans +'};
const D={natural_hazards:'Risques naturels',weather_climate:'Climat & météo',cyber_technology:'Technologie',public_health:'Santé',financial_stress:'Finance',energy:'Énergie',economy_labor:'Économie & emploi',supply_fuel:'Logistique',social_collective_behavior:'Société',geopolitics_security:'Géopolitique',regulation_policy:'Régulation',transport_mobility:'Transport'};
const COLORS=['#ffc85a','#7b7cff','#4f8dff','#46d7ff','#44dda2','#bb7cff','#ff8c72','#8fc8ff','#7ee2bc','#ffb36b','#6ca6ff','#c77cff'];
const state={rows:[],query:'',horizon:'all',limit:10,hero:null};
const title=f=>String(f?.title||f?.headline||f?.outcome||'Scénario');
const summary=f=>String(f?.summary||f?.public_summary||f?.why_now||'');
const tier=f=>String(f?.horizon_tier||f?.time_window?.tier||'near').toLowerCase();
const prob=f=>Math.max(0,Math.min(100,Math.round(Number(f?.probability?.percent??(Number(f?.probability?.estimate)||0)*100)||0)));
const conf=f=>Math.max(0,Math.min(100,Math.round(Number(f?.consolidation?.score??f?.confidence??0)||0)));
const providers=f=>Array.isArray(f?.consolidation?.source_providers)?f.consolidation.source_providers:[];
const active=f=>!['resolved','invalidated','expired'].includes(String(f?.status||'').toLowerCase());
const score=f=>prob(f)+conf(f)*.2+Number(f?.commercial_priority||.5)*8+Number(f?.probability_delta_points||0)*1.2;
const clip=(v,n)=>{v=String(v||'').trim();return v.length>n?v.slice(0,n-1)+'…':v};
const looksEnglish=v=>{const s=` ${String(v||'').toLowerCase()} `;return /\b(will|what|when|who|how|world|global|climate change|artificial intelligence|paper published|damage incurred|provide|develop|before|after|between|highest score|public health emergency|flood in|wildfire in|earthquake in|storm in)\b/.test(s)};
function frRegion(v){let s=String(v||'Monde').trim();const exact={'United States':'États-Unis','United Kingdom':'Royaume-Uni','South Korea':'Corée du Sud','North Korea':'Corée du Nord','European Union':'Union européenne','World':'Monde','Global':'Monde','Nepal':'Népal','Spain':'Espagne','Germany':'Allemagne','Italy':'Italie','Japan':'Japon','China':'Chine'};if(exact[s])return exact[s];s=s.replace(/^Flood in\s+/i,'Inondations · ').replace(/^Wildfire in\s+/i,'Incendies · ').replace(/^Storm in\s+/i,'Tempête · ').replace(/^Earthquake in\s+/i,'Séisme · ');return s;}
function frTitle(f){const raw=title(f);if(!looksEnglish(raw)&&!/^Découverte scientifique\s*:\s*[A-Z].*\b(of|and|for|with|under)\b/i.test(raw)&&!/^Recherche émergente\s*:\s*[A-Z].*\b(of|and|for|with|under)\b/i.test(raw))return raw;const l=raw.toLowerCase();if(l.includes('greenland'))return 'Les États-Unis obtiendront-ils une souveraineté formelle sur une partie du Groenland en 2026 ?';if(l.includes('climate change')&&l.includes('gdp'))return 'Quel sera le coût économique total du changement climatique au XXIᵉ siècle ?';if(l.includes('nature, science, or cell')||l.includes('formally credited author'))return 'Une IA sera-t-elle créditée comme auteur scientifique dans Nature, Science ou Cell avant 2030 ?';if(l.includes('nuclear fusion')&&l.includes('10%'))return 'Quand la fusion nucléaire fournira-t-elle plus de 10 % de l’énergie primaire mondiale ?';if(l.includes('russia')&&l.includes('ukraine'))return 'Comment évoluera la guerre Russie–Ukraine sur la période suivie ?';if(l.includes('who declare')||l.includes('public health emergency of international concern'))return 'L’OMS déclarera-t-elle une urgence de santé publique de portée internationale ?';if(l.includes('first artificial general intelligence')||l.includes('first agi'))return 'Qui développera la première intelligence artificielle générale ?';if(/^découverte scientifique/i.test(raw))return `${D[f.domain]||'Recherche'} : une avancée scientifique récente pourrait ouvrir une nouvelle phase d’application.`;if(/^recherche émergente/i.test(raw))return `${D[f.domain]||'Recherche'} : une nouvelle piste de recherche pourrait franchir une étape d’application.`;return `${D[f.domain]||'Prospective'} · ${frRegion(f?.region||f?.geography||'Monde')} : scénario prospectif à ${H[tier(f)]||'horizon actif'}.`;}
function frSummary(f){const raw=summary(f);if(raw&&!looksEnglish(raw))return raw;return `Providence suit cette trajectoire à partir de signaux convergents et réévalue sa probabilité jusqu’à l’échéance ${H[tier(f)]||'prévue'}.`;}
function horizonGroup(f){const t=tier(f),label=String(f?.horizon_label||'').toLowerCase(),s=`${t} ${label}`;if(/immediate|very_short|hour|72/.test(s))return'immediate';if(/near|short|day|week/.test(s))return'near';if(/medium|month/.test(s))return'medium';return'years';}
function diversified(rows,limit=18){const sorted=[...rows].sort((a,b)=>score(b)-score(a)),out=[],domains=new Map(),tiers=new Map();for(const f of sorted){const d=domains.get(f.domain)||0,h=tiers.get(tier(f))||0;if(d>=3||h>=5)continue;out.push(f);domains.set(f.domain,d+1);tiers.set(tier(f),h+1);if(out.length>=limit)break;}for(const f of sorted){if(out.length>=limit)break;if(!out.includes(f))out.push(f);}return out;}
function visibleRows(){const q=state.query.trim().toLowerCase();return state.rows.filter(f=>{if(state.horizon!=='all'&&horizonGroup(f)!==state.horizon)return false;if(!q)return true;return `${frTitle(f)} ${frRegion(f?.region||f?.geography||'')} ${D[f.domain]||f.domain||''}`.toLowerCase().includes(q);});}
function setText(sel,value){const n=$(sel);if(n&&n.textContent!==String(value))n.textContent=String(value)}
function setHero(f,{scroll=false}={}){if(!f)return;state.hero=f;const delta=Number(f?.signal_convergence?.probability_delta_points??f?.probability_delta_points??0);const hero=$('#futureLead');if(hero)hero.dataset.tone=prob(f)>=70?'high':prob(f)>=45?'mid':'low';setText('#futureTitle',frTitle(f));setText('#futureProbability',`${prob(f)}%`);setText('#futureDelta',delta?`${delta>0?'↑ +':'↓ '}${Math.abs(delta)} pts après convergence`:'Estimation stable');setText('#futureRegion',`${frRegion(f?.region||f?.geography||'Monde')} · ${H[tier(f)]||f?.horizon_label||tier(f)}`);setText('#futureSummary',frSummary(f));setText('#futureConfidence',`${conf(f)||'—'}/100`);setText('#futureStrong',String(f?.signal_convergence?.strong_signals?.length||f?.favorable_signals?.length||0));setText('#futureSources',String(providers(f).length||'—'));if(scroll)hero?.scrollIntoView({behavior:'smooth',block:'start'});renderFeed(false);}
function card(f,i){const c=COLORS[i%COLORS.length],selected=state.hero===f?' is-selected':'';return `<article class="p1615-card${selected}" style="--tone:${c};--p:${prob(f)}" role="button" tabindex="0" data-p1615-index="${i}" aria-label="Afficher ${esc(frTitle(f))}"><span class="p1615-tag">${esc(D[f.domain]||f.domain||'Prévision')}</span><h3>${esc(frTitle(f))}</h3><div class="p1615-meta">${esc(frRegion(f?.region||f?.geography||'Monde'))} · ${esc(H[tier(f)]||f?.horizon_label||tier(f))}</div><div class="p1615-prob"><b>${prob(f)}%</b></div><div class="p1615-foot"><span>Solidité <strong>${conf(f)||'—'}/100</strong></span><span class="p1615-open">Voir →</span></div></article>`;}
function renderFeed(resetLimit=false){const host=$('#p1615FeedGrid'),count=$('#p1615Count'),more=$('#p1615More');if(!host)return;if(resetLimit)state.limit=matchMedia('(max-width:760px)').matches?8:12;const rows=visibleRows();if(count)count.textContent=String(rows.length);const shown=diversified(rows,Math.min(rows.length,state.limit));host.innerHTML=shown.length?shown.map(card).join(''):'<div class="p1615-empty">Aucune prévision ne correspond à ce filtre.</div>';$$('[data-p1615-index]',host).forEach((el,i)=>{const f=shown[i],open=()=>setHero(f,{scroll:true});el.addEventListener('click',open);el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open();}})});if(more){more.hidden=rows.length<=shown.length;more.textContent=`Afficher plus (${Math.max(0,rows.length-shown.length)})`;}}
function renderTimeline(){const host=$('#temporalField');if(!host)return;const rows=diversified(state.horizon==='all'?state.rows:state.rows.filter(f=>horizonGroup(f)===state.horizon),8);const W=900,HGT=285,origin={x:66,y:143},xs={immediate:205,near:335,medium:470,long:610,strategic:745,deep:850},ys=[48,77,107,137,167,197,227,252];let svg=`<svg viewBox="0 0 ${W} ${HGT}" role="img" aria-label="Futurs plausibles à partir du présent">`;[['immediate','≤72 h'],['near','Semaines'],['medium','Mois'],['long','1–3 ans'],['strategic','3–5 ans'],['deep','5 ans +']].forEach(([k,l])=>{const x=xs[k];svg+=`<line class="p1615-axis" x1="${x}" y1="28" x2="${x}" y2="268"/><text class="p1615-axis-label" x="${x+5}" y="20">${l}</text>`});svg+=`<circle class="p1615-now-ring" cx="${origin.x}" cy="${origin.y}" r="16"/><circle class="p1615-now-core" cx="${origin.x}" cy="${origin.y}" r="5"/><text class="p1615-now-text" x="27" y="177">MAINTENANT</text>`;rows.forEach((f,i)=>{const x=xs[tier(f)]||xs.near,y=ys[i]||140,c=COLORS[i%COLORS.length],w=(2.3+prob(f)/40).toFixed(1),c1=Math.min(x-80,145),c2=Math.max(origin.x+75,x-110),label=clip(frTitle(f),43),sub=`${prob(f)}% · ${frRegion(f?.region||f?.geography||'Monde')}`;svg+=`<g class="p1615-branch-group" data-p1615-branch="${i}" style="--branch:${c};--weight:${w}px"><path class="p1615-branch" d="M ${origin.x} ${origin.y} C ${c1} ${origin.y}, ${c2} ${y}, ${x} ${y}"/><path class="p1615-branch-hit" d="M ${origin.x} ${origin.y} C ${c1} ${origin.y}, ${c2} ${y}, ${x} ${y}"/><circle class="p1615-branch-dot" cx="${x}" cy="${y}" r="7"/><text class="p1615-branch-label" x="${Math.min(x+12,768)}" y="${y-3}">${esc(label)}</text><text class="p1615-branch-sub" x="${Math.min(x+12,768)}" y="${y+13}">${esc(sub)}</text></g>`});svg+='</svg>';host.innerHTML=svg;$$('[data-p1615-branch]',host).forEach((g,i)=>{const open=()=>{$$('[data-p1615-branch]',host).forEach(x=>{x.classList.toggle('is-muted',x!==g);x.classList.toggle('is-active',x===g)});setHero(rows[i],{scroll:true});};g.addEventListener('click',open);});}
function bind(){const search=$('#p1615Search');search?.addEventListener('input',e=>{state.query=e.target.value||'';renderFeed(true)});$$('.p1615-chip').forEach(ch=>ch.addEventListener('click',()=>{state.horizon=ch.dataset.horizon||'all';$$('.p1615-chip').forEach(x=>x.classList.toggle('is-active',x===ch));renderFeed(true);renderTimeline()}));$('#p1615More')?.addEventListener('click',()=>{state.limit+=matchMedia('(max-width:760px)').matches?8:12;renderFeed(false)});}
async function boot(){try{const r=await fetch('/api/snapshot',{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);const data=await r.json();state.rows=(data.forecasts||[]).filter(active).sort((a,b)=>score(b)-score(a));state.limit=matchMedia('(max-width:760px)').matches?8:12;if(state.rows.length)setHero(state.rows[0]);renderFeed(false);renderTimeline();bind();}catch(e){const host=$('#p1615FeedGrid');if(host)host.innerHTML='<div class="p1615-empty">Les prévisions sont momentanément indisponibles.</div>';}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();