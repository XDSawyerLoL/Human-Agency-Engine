(()=>{'use strict';
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const H={immediate:'≤ 72 h',near:'Jours à semaines',medium:'Mois',long:'1–3 ans',strategic:'3–5 ans',deep:'5 ans +'};
const D={natural_hazards:'Risques naturels',weather_climate:'Climat',cyber_technology:'Technologie',public_health:'Santé',financial_stress:'Finance',energy:'Énergie',economy_labor:'Économie',supply_fuel:'Logistique',social_collective_behavior:'Société',geopolitics_security:'Géopolitique',regulation_policy:'Régulation',transport_mobility:'Transport'};
const p=f=>Math.max(0,Math.min(100,Math.round(Number(f?.probability?.percent??(Number(f?.probability?.estimate)||0)*100)||0))),conf=f=>Math.max(0,Math.min(100,Math.round(Number(f?.consolidation?.score??f?.confidence??0)||0))),title=f=>f?.title||f?.headline||f?.outcome||'Scénario',tier=f=>String(f?.horizon_tier||f?.time_window?.tier||'near').toLowerCase(),summary=f=>f?.summary||f?.public_summary||f?.why_now||'',active=f=>!['resolved','invalidated','expired'].includes(String(f?.status||'').toLowerCase()),conv=f=>f?.signal_convergence||{},strong=f=>conv(f).strong_signals||[],weak=f=>conv(f).weak_signals||[],contrary=f=>f?.contrary_signals||conv(f).contrary_signals||[],providers=f=>f?.consolidation?.source_providers||[];
const score=f=>p(f)+conf(f)*.25+Number(f?.commercial_priority||.5)*8+Number(conv(f).probability_delta_points||0)*2;
const clip=(v,n=76)=>{v=String(v||'');return v.length>n?v.slice(0,n-1)+'…':v};
const balance=f=>{const up=strong(f).length,down=contrary(f).length,total=Math.max(1,up+down),value=Math.round((up-down)/total*50);return {up,down,value};};
const horizonMatch=(f,key)=>{const t=tier(f),label=String(f?.horizon_label||'').toLowerCase();if(!key||key==='all')return true;if(key==='immediate')return /immediate|very_short|hour|72/.test(`${t} ${label}`);if(key==='near')return /near|short|day|week/.test(`${t} ${label}`)&&!/very_short|immediate/.test(`${t} ${label}`);if(key==='medium')return /medium|month/.test(`${t} ${label}`);if(key==='long')return /long|year/.test(`${t} ${label}`)&&!/strategic|deep|5\+/.test(`${t} ${label}`);if(key==='deep')return /strategic|deep|5\+|multi.?year/.test(`${t} ${label}`);return true;};
function lead(f,track,snapshot){const c=conv(f),delta=Number(c.probability_delta_points??f?.probability_delta_points??0),hero=$('#futureLead');if(hero){hero.dataset.tone=p(f)>=70?'high':p(f)>=45?'mid':'low';hero.style.setProperty('--probability',String(p(f)));}$('#futureTitle').textContent=title(f);$('#futureProbability').textContent=`${p(f)}%`;$('#futureDelta').textContent=delta?`${delta>0?'↑ +':'↓ '}${Math.abs(delta)} pts après convergence`:'Estimation stable';$('#futureRegion').textContent=`${f?.region||f?.geography||'Monde'} · ${H[tier(f)]||f?.horizon_label||tier(f)}`;$('#futureSummary').textContent=summary(f)||'Providence relie les signaux actifs, leur indépendance et les mécanismes causaux compatibles.';$('#futureConfidence').textContent=`${conf(f)||'—'}/100`;$('#futureStrong').textContent=strong(f).length;$('#futureSources').textContent=providers(f).length||snapshot?.summary?.source_providers||'—';
 const s=strong(f),w=weak(f),causal=(f?.causal_chain||[]).filter(Boolean);const nodes=[s[0]?.title||f?.evidence?.[0]?.title||'Signal convergent',causal[0]||c.explanation||'Mécanisme compatible',title(f),w[0]?.title||contrary(f)[0]?.title||'Contexte faible',providers(f)[0]?.label||'Source indépendante'];['#futureNode1','#futureNode2','#futureNode3','#futureNode4','#futureNode5'].forEach((id,i)=>{const n=$(id);if(n)n.textContent=clip(nodes[i],42)});const core=$('#futureCore');if(core)core.innerHTML=`${p(f)}%<br><small>${esc(D[f.domain]||f.domain||'Prévision')}</small>`;
 const ece=Number(track?.calibration?.global?.ece??track?.calibration?.ece??track?.ece),calibrationScore=Number.isFinite(ece)?Math.max(0,Math.min(100,Math.round((1-(ece<=1?ece:ece/100))*100))):null,calibrationNode=$('#futureCalibration');if(calibrationNode){calibrationNode.textContent=calibrationScore!==null?`${calibrationScore}%`:'—';calibrationNode.closest('.p15-gauge')?.style.setProperty('--gauge',String(calibrationScore??0));}
 const drivers=[...s.slice(0,3).map(x=>({name:x.title||x.label||'Signal fort',trend:'↗',impact:'Élevé'})),...contrary(f).slice(0,2).map(x=>({name:x.title||x.label||'Contre-signal',trend:'↘',impact:'Contraire'})),...w.slice(0,1).map(x=>({name:x.title||x.label||'Signal faible',trend:'→',impact:'Faible'}))],tbody=$('#futureDrivers tbody');if(tbody)tbody.innerHTML=(drivers.length?drivers:[{name:'Convergence multi-source',trend:'→',impact:'Moyen'}]).map(x=>`<tr><td>${esc(clip(x.name,46))}</td><td>${x.trend}</td><td class="${x.impact==='Élevé'?'p15-impact-high':x.impact==='Moyen'?'p15-impact-mid':'p15-impact-low'}">${x.impact}</td></tr>`).join('');
 const alerts=[...(snapshot.forecasts||[])].filter(active).filter(x=>Number(x.probability_delta_points)>0||x.probability_direction==='rising').sort((a,b)=>Number(b.probability_delta_points||0)-Number(a.probability_delta_points||0)).slice(0,5),alertHost=$('#futureAlerts');if(alertHost)alertHost.innerHTML=alerts.map(x=>`<div class="p15-list-row"><span class="dot">△</span><div><b>${esc(clip(title(x),45))}</b><small>${esc(x.region||'Monde')}</small></div><strong>${Number(x.probability_delta_points)>0?`+${Number(x.probability_delta_points)} pts`:`${p(x)}%`}</strong></div>`).join('')||'<small style="color:#8297ab">Aucune alerte montante.</small>';
 const catalog=snapshot?.summary?.source_catalog||[],sourceHost=$('#futureSourcesStrip');if(sourceHost)sourceHost.innerHTML=catalog.filter(x=>x.active).slice(0,8).map(x=>`<article class="p15-source"><b>${esc(x.label||x.key)}</b><small>${esc(x.role||'Source active')}</small></article>`).join('')||providers(f).slice(0,8).map(x=>`<article class="p15-source"><b>${esc(x.label||x.key||x)}</b><small>Source contributrice</small></article>`).join('');}
function card(f,i,rowIndex){const b=balance(f),negative=b.value<0;return `<article class="p15-forecast-card p168-selectable" style="--ring:${['#a15cff','#20c9ff','#3de79a'][i%3]}" role="button" tabindex="0" data-pick="${rowIndex}" aria-label="Afficher la prévision ${esc(title(f))}"><small>${esc(D[f.domain]||f.domain||'PRÉVISION')}</small><h3>${esc(title(f))}</h3><p>${esc(f.region||'Monde')} · ${esc(H[tier(f)]||f?.horizon_label||tier(f))}</p><div class="p15-prob-ring" style="--p:${p(f)};--ring:${['#a15cff','#20c9ff','#3de79a'][i%3]}"><b>${p(f)}%</b></div><div class="p15-mini-meta"><span>Solidité ${conf(f)||'—'}/100</span><span>${providers(f).length} sources</span></div><div class="p168-signalbar ${negative?'negative':''}"><span>− ${b.down}</span><div class="track"><i style="--balance:${Math.abs(b.value)}%"></i></div><span>+ ${b.up}</span></div><div class="p168-card-foot"><span>Voir le détail</span><i aria-hidden="true">→</i></div></article>`}
async function run(){try{const [r,t]=await Promise.all([fetch('/api/snapshot',{cache:'no-store'}),fetch('/api/track-record',{cache:'no-store'})]);if(!r.ok)throw new Error(`HTTP ${r.status}`);const snapshot=await r.json(),track=t.ok?await t.json():{},rows=(snapshot.forecasts||[]).filter(active).sort((a,b)=>score(b)-score(a));if(!rows.length){$('#futureTitle').textContent='Aucune prévision majeure active';$('#futureSummary').textContent='Providence continue d’observer les signaux. Aucune estimation de remplacement n’est inventée.';return}
 const priorityHost=$('#futurePriority');
 const selectCard=cardEl=>{const f=rows[Number(cardEl.dataset.pick)];if(!f)return;lead(f,track,snapshot);$$('.p15-forecast-card').forEach(x=>x.classList.remove('selected'));cardEl.classList.add('selected');};
 const bindCards=()=>{$$('[data-pick]').forEach(cardEl=>{if(cardEl.dataset.bound==='1')return;cardEl.dataset.bound='1';cardEl.addEventListener('click',()=>selectCard(cardEl));cardEl.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();selectCard(cardEl);}});});};
 const setRail=(key)=>{const rail=$('.p16-time-rail');if(!rail)return;$$('.p16-time-rail a').forEach(a=>{let k='all';try{k=new URL(a.href,location.href).searchParams.get('horizon')||'all'}catch{}const on=k===key;a.classList.toggle('active',on);if(on)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current');});const activeNode=$('.p16-time-rail a.active');activeNode?.scrollIntoView({behavior:'smooth',block:'nearest',inline:'center'});};
 const renderHorizon=(key,{updateUrl=false}={})=>{const filtered=key==='all'?rows:rows.filter(f=>horizonMatch(f,key));setRail(key);if(updateUrl){const u=new URL(location.href);if(key==='all')u.searchParams.delete('horizon');else u.searchParams.set('horizon',key);history.replaceState({horizon:key},'',u);}if(!filtered.length){$('#futureTitle').textContent='Aucune prévision publiée sur cet horizon';$('#futureProbability').textContent='—';$('#futureDelta').textContent='Aucune estimation active';$('#futureRegion').textContent=key==='all'?'Tous horizons':key;$('#futureSummary').textContent='Providence n’invente pas de scénario de remplacement lorsqu’aucune prévision active ne correspond à cet horizon.';if(priorityHost)priorityHost.innerHTML='<div class="p16-horizon-empty">Aucun autre scénario actif sur cet horizon.</div>';return;}lead(filtered[0],track,snapshot);if(priorityHost)priorityHost.innerHTML=filtered.slice(1,4).map((f,i)=>card(f,i,rows.indexOf(f))).join('')||'<div class="p16-horizon-empty">Aucun scénario concurrent actif sur cet horizon.</div>';bindCards();};
 $$('.p16-time-rail a').forEach(a=>{a.addEventListener('click',e=>{e.preventDefault();let key='all';try{key=new URL(a.href,location.href).searchParams.get('horizon')||'all'}catch{}renderHorizon(key,{updateUrl:true});});});
 window.addEventListener('popstate',()=>renderHorizon(new URL(location.href).searchParams.get('horizon')||'all'));
 const initial=new URL(location.href).searchParams.get('horizon')||'all';renderHorizon(initial);
}catch(e){$('#futureTitle').textContent='Champ prédictif indisponible';$('#futureSummary').textContent=e.message}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run();
})();