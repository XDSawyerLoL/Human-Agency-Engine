(()=>{'use strict';
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const clamp=(v,a,b)=>Math.max(a,Math.min(b,Number(v)||0));
function quality(label,value,note){return `<div class="pv-quality-item"><div><b>${esc(label)}</b><strong>${Math.round(value)}/100</strong></div><div class="pv-quality-track"><i style="width:${clamp(value,0,100)}%"></i></div><small>${esc(note)}</small></div>`;}
(async()=>{try{const r=await fetch('/api/snapshot',{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);const d=await r.json(),s=d.summary||{},rows=(d.forecasts||[]).filter(f=>!['resolved','invalidated'].includes(f.status)),k=$('#sourceKpis').querySelectorAll('strong');const domains=new Set(rows.map(f=>f.domain).filter(Boolean));const trusts=new Map();for(const f of rows)for(const e of f.evidence||[]){if(!e.source_key||!Number.isFinite(Number(e.source_trust)))continue;trusts.set(e.source_key,Math.max(trusts.get(e.source_key)||0,Number(e.source_trust)));}const avgTrust=trusts.size?[...trusts.values()].reduce((a,b)=>a+b,0)/trusts.size:0;k[0].textContent=s.source_providers??trusts.size;k[1].textContent=s.source_families??'—';k[2].textContent=s.signals_considered??'—';k[3].textContent=domains.size;k[4].textContent=trusts.size?`${Math.round(avgTrust*100)}%`:'—';const trace=rows.length?rows.filter(f=>(f.evidence||[]).length&&f.falsification).length/rows.length*100:0;const convergence=rows.length?rows.filter(f=>(f.signal_convergence?.strong_signals||[]).length>0).length/rows.length*100:0;const diversity=Math.min(100,(Number(s.source_families)||0)*12.5);const coverage=Math.min(100,domains.size/12*100);$('#sourceQuality').innerHTML=[quality('Fiabilité',avgTrust*100,`${trusts.size} sources avec confiance publiée`),quality('Diversité',diversity,`${s.source_families??0} familles de sources`),quality('Couverture',coverage,`${domains.size}/12 domaines actifs`),quality('Traçabilité',trace,'prévisions avec preuves + réfutation'),quality('Convergence',convergence,'prévisions soutenues inter-domaines')].join('');}catch(e){$('#sourceQuality').innerHTML=`<div class="v4-empty">Qualité indisponible : ${esc(e.message)}</div>`;}})();})();