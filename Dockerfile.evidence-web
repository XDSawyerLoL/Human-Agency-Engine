FROM nginx:1.27-alpine

COPY hostinger/evidence-nginx.conf /etc/nginx/conf.d/default.conf
COPY index.html /usr/share/nginx/html/index.html
COPY evidence-public.css /usr/share/nginx/html/evidence-public.css
COPY evidence-public.js /usr/share/nginx/html/evidence-public.js

# Hostinger serves the generated snapshot locally. The source repository keeps
# the legacy GitHub URL only for backwards compatibility with old Pages builds.
RUN sed -i 's#const SNAPSHOT_URL = .*#const SNAPSHOT_URL = "./data/evidence-live.json";#' /usr/share/nginx/html/evidence-public.js \
    && mkdir -p /usr/share/nginx/html/data

EXPOSE 80

HEALTHCHECK --interval=30s --timeout=5s --retries=5 --start-period=10s \
  CMD wget -q -O /dev/null http://127.0.0.1/ || exit 1
